diff --git a/python/sglang/kernels/jit/csrc/elementwise/fused_qknorm_rope.cuh b/python/sglang/kernels/jit/csrc/elementwise/fused_qknorm_rope.cuh
index 1c1f41dccf..cf82294a82 100644
--- a/python/sglang/kernels/jit/csrc/elementwise/fused_qknorm_rope.cuh
+++ b/python/sglang/kernels/jit/csrc/elementwise/fused_qknorm_rope.cuh
@@ -75,7 +75,7 @@ __device__ inline float compute_freq(float base, int rotary_dim, int half_dim, f
 // interleave (GPT-J) pairs (2k,2k+1) share the same freq/theta,
 //   so sin/cos is computed once per pair and copied to the odd element,
 //   halving powf + __sincosf calls vs a naive per-element approach.
-template <int head_dim, bool interleave, bool yarn>
+template <int head_dim, bool interleave, bool yarn, bool add_one = false>
 __global__ void fusedQKNormRopeKernel(
     __nv_bfloat16* qkv,  // [num_tokens, (nq+nk+nv)*head_dim], in-place
     int const num_heads_q,
@@ -146,7 +146,15 @@ __global__ void fusedQKNormRopeKernel(
     vec_T wvec;
     wvec.load((isQ ? q_weight : k_weight) + offsetThread - offsetWarp);
     for (int i = 0; i < numElemsPerThread; i++) {
-      elements[i] *= rms_rcp * device::cast<float>(wvec[i]);
+      // Gemma-family norms scale by (1 + w) rather than w. Doing the add here
+      // keeps it in fp32: folding it into a bf16 weight on the host costs ~4%
+      // relative error, because near 1.0 a bf16 ULP is 2^-7 and every weight
+      // lands next to 1.0 after the add.
+      float w = device::cast<float>(wvec[i]);
+      if constexpr (add_one) {
+        w += 1.0f;
+      }
+      elements[i] *= rms_rcp * w;
     }
   }
 
@@ -292,6 +300,10 @@ void fused_qk_norm_rope(
       JIT_HEAD_DIM == 64 || JIT_HEAD_DIM == 128 || JIT_HEAD_DIM == 256, "JIT_HEAD_DIM must be 64, 128, or 256");
   static_assert(JIT_INTERLEAVE == 0 || JIT_INTERLEAVE == 1, "JIT_INTERLEAVE must be 0 or 1");
   static_assert(JIT_YARN == 0 || JIT_YARN == 1, "JIT_YARN must be 0 or 1");
+#ifndef JIT_ADD_ONE
+#define JIT_ADD_ONE 0
+#endif
+  static_assert(JIT_ADD_ONE == 0 || JIT_ADD_ONE == 1, "JIT_ADD_ONE must be 0 or 1");
   RuntimeCheck(head_dim == JIT_HEAD_DIM, "head_dim mismatch with JIT-compiled kernel");
 
   int numElemsPerThread = head_dim / 32;
@@ -324,7 +336,11 @@ void fused_qk_norm_rope(
   auto const* kw_ptr = reinterpret_cast<__nv_bfloat16 const*>(k_weight.data_ptr());
   auto const* pos_ptr = reinterpret_cast<int const*>(position_ids.data_ptr());
 
-  fusedQKNormRopeKernel<JIT_HEAD_DIM, static_cast<bool>(JIT_INTERLEAVE), static_cast<bool>(JIT_YARN)>
+  fusedQKNormRopeKernel<
+      JIT_HEAD_DIM,
+      static_cast<bool>(JIT_INTERLEAVE),
+      static_cast<bool>(JIT_YARN),
+      static_cast<bool>(JIT_ADD_ONE)>
       <<<gridSize, blockSize, 0, stream>>>(
           qkv_ptr,
           num_heads_q,
diff --git a/python/sglang/kernels/ops/attention/fused_qknorm_rope.py b/python/sglang/kernels/ops/attention/fused_qknorm_rope.py
index ca35fbd610..e9a7bdeb61 100644
--- a/python/sglang/kernels/ops/attention/fused_qknorm_rope.py
+++ b/python/sglang/kernels/ops/attention/fused_qknorm_rope.py
@@ -13,12 +13,15 @@ if TYPE_CHECKING:
 
 
 @cache_once
-def _jit_fused_qknorm_rope_module(head_dim: int, is_neox: bool, yarn: bool) -> Module:
+def _jit_fused_qknorm_rope_module(
+    head_dim: int, is_neox: bool, yarn: bool, add_one: bool = False
+) -> Module:
     return load_jit(
         "fused_qknorm_rope",
         head_dim,
         int(is_neox),
         int(yarn),
+        int(add_one),
         cuda_files=["elementwise/fused_qknorm_rope.cuh"],
         cuda_wrappers=[("fused_qk_norm_rope", "fused_qk_norm_rope")],
         extra_cuda_cflags=[
@@ -26,6 +29,7 @@ def _jit_fused_qknorm_rope_module(head_dim: int, is_neox: bool, yarn: bool) -> M
             f"-DJIT_HEAD_DIM={head_dim}",
             f"-DJIT_INTERLEAVE={0 if is_neox else 1}",
             f"-DJIT_YARN={1 if yarn else 0}",
+            f"-DJIT_ADD_ONE={1 if add_one else 0}",
         ],
     )
 
@@ -51,6 +55,7 @@ def fused_qk_norm_rope_out(
     high: float,
     attention_factor: float,
     rotary_dim: int,
+    add_one: bool = False,
 ) -> None:
     """
     Fused QK RMSNorm + RoPE applied in-place on the QKV tensor.
@@ -76,7 +81,7 @@ def fused_qk_norm_rope_out(
         rotary_dim:       number of elements per head to apply RoPE to
     """
     yarn = factor != 1.0
-    module = _jit_fused_qknorm_rope_module(head_dim, is_neox, yarn)
+    module = _jit_fused_qknorm_rope_module(head_dim, is_neox, yarn, add_one)
     module.fused_qk_norm_rope(
         qkv,
         q_weight,
@@ -99,7 +104,11 @@ def fused_qk_norm_rope_out(
 
 @cache_once
 def can_use_fused_qk_norm_rope(
-    head_dim: int, is_neox: bool, dtype: torch.dtype, yarn: bool = False
+    head_dim: int,
+    is_neox: bool,
+    dtype: torch.dtype,
+    yarn: bool = False,
+    add_one: bool = False,
 ) -> bool:
     """Return True if the JIT fused QK-Norm + RoPE kernel can be used.
 
@@ -120,7 +129,7 @@ def can_use_fused_qk_norm_rope(
         logger.warning(f"Unsupported dtype={dtype} for JIT fused_qk_norm_rope kernel")
         return False
     try:
-        _jit_fused_qknorm_rope_module(head_dim, is_neox, yarn)
+        _jit_fused_qknorm_rope_module(head_dim, is_neox, yarn, add_one)
         return True
     except Exception as e:
         logger.warning(f"Failed to load JIT fused_qk_norm_rope kernel: {e}")
@@ -144,6 +153,7 @@ def fused_qk_norm_rope(
     high: float,
     attention_factor: float,
     rotary_dim: Optional[int] = None,
+    add_one: bool = False,
 ) -> None:
     """
     Fused QK RMSNorm + RoPE applied in-place on the QKV tensor.
@@ -187,4 +197,5 @@ def fused_qk_norm_rope(
         high,
         attention_factor,
         rotary_dim,
+        add_one,
     )
diff --git a/python/sglang/srt/models/gemma3_causal.py b/python/sglang/srt/models/gemma3_causal.py
index 2dbf1624e1..6bbf8c2eb1 100644
--- a/python/sglang/srt/models/gemma3_causal.py
+++ b/python/sglang/srt/models/gemma3_causal.py
@@ -47,11 +47,18 @@ from sglang.srt.model_loader.weight_utils import (
     maybe_remap_kv_scale_name,
 )
 from sglang.srt.runtime_context import get_parallel
-from sglang.srt.utils import add_prefix, cpu_has_amx_support, is_cpu, make_layers
+from sglang.srt.utils import add_prefix, cpu_has_amx_support, is_cpu, is_cuda, make_layers
 from sglang.srt.utils.hf_transformers.common import _resolve_local_or_cached_file
 
 _is_cpu = is_cpu()
 _is_cpu_amx_available = cpu_has_amx_support()
+_is_cuda = is_cuda()
+
+if _is_cuda:
+    from sglang.kernels.ops.attention.fused_qknorm_rope import (
+        can_use_fused_qk_norm_rope,
+        fused_qk_norm_rope,
+    )
 
 
 # Aligned with HF's implementation, using sliding window inclusive with the last token
@@ -242,6 +249,19 @@ class Gemma3Attention(nn.Module):
         self.q_norm = Gemma3RMSNorm(dim=config.head_dim, eps=config.rms_norm_eps)
         self.k_norm = Gemma3RMSNorm(dim=config.head_dim, eps=config.rms_norm_eps)
 
+        # sglang ships a kernel that does q_norm, k_norm and rope in one launch.
+        # It needs the full rotary range, a head_dim it has a specialisation for
+        # and bf16; Gemma applies (1 + w) rather than w, which the kernel does
+        # under add_one. Resolved once here so the forward path is a bool test.
+        self.use_fused_qk_norm_rope = False
+        if _is_cuda and self.rotary_dim == self.head_dim:
+            self.use_fused_qk_norm_rope = can_use_fused_qk_norm_rope(
+                self.head_dim,
+                is_neox=True,
+                dtype=torch.bfloat16,
+                add_one=True,
+            )
+
     def forward_cpu(
         self,
         positions: torch.Tensor,
@@ -282,25 +302,55 @@ class Gemma3Attention(nn.Module):
     ) -> torch.Tensor:
         qkv, _ = self.qkv_proj(hidden_states)
         # [s, h * head_dim]
-        q, k, v = qkv.split([self.q_size, self.kv_size, self.kv_size], dim=-1)
 
-        # [s, h, head_dim]
-        q = q.unflatten(-1, (self.num_heads, self.head_dim))
-        # -> [h, s, head_dim]
-        q = q.transpose(0, 1).unsqueeze(0)
-        q = self.q_norm(q)
-        k = k.unflatten(-1, (self.num_kv_heads, self.head_dim))
-        # -> [h, s, head_dim]
-        k = k.transpose(0, 1).unsqueeze(0)
-        k = self.k_norm(k)
-
-        # q, k = self.rotary_emb(positions, q, k)
-        cos, sin = position_embeddings
-        q, k = apply_rotary_pos_emb(q, k, cos, sin)
-
-        # [b, h, s, head_dim] ->  [b, s, h, head_dim]
-        q = q.permute(0, 2, 1, 3)
-        k = k.permute(0, 2, 1, 3)
+        if self.use_fused_qk_norm_rope and qkv.dtype == torch.bfloat16:
+            # One launch for both norms and the rotation, in place on qkv. The
+            # kernel reads the raw Gemma weights and applies (1 + w) itself.
+            # The model reshapes positions to [1, s] for Gemma3RotaryEmbedding;
+            # the kernel wants the flat [s] form.
+            qkv = qkv.contiguous()
+            fused_qk_norm_rope(
+                qkv,
+                self.num_heads,
+                self.num_kv_heads,
+                self.num_kv_heads,
+                self.head_dim,
+                self.q_norm.eps,
+                self.q_norm.weight.data,
+                self.k_norm.weight.data,
+                float(self.rope_theta),
+                True,  # Gemma-3 is NeoX style
+                positions.reshape(-1).to(torch.int32),
+                1.0,  # factor: no YaRN
+                0.0,  # low
+                0.0,  # high
+                1.0,  # attention_factor
+                rotary_dim=self.rotary_dim,
+                add_one=True,
+            )
+            q, k, v = qkv.split([self.q_size, self.kv_size, self.kv_size], dim=-1)
+            q = q.unflatten(-1, (self.num_heads, self.head_dim)).unsqueeze(0)
+            k = k.unflatten(-1, (self.num_kv_heads, self.head_dim)).unsqueeze(0)
+        else:
+            q, k, v = qkv.split([self.q_size, self.kv_size, self.kv_size], dim=-1)
+
+            # [s, h, head_dim]
+            q = q.unflatten(-1, (self.num_heads, self.head_dim))
+            # -> [h, s, head_dim]
+            q = q.transpose(0, 1).unsqueeze(0)
+            q = self.q_norm(q)
+            k = k.unflatten(-1, (self.num_kv_heads, self.head_dim))
+            # -> [h, s, head_dim]
+            k = k.transpose(0, 1).unsqueeze(0)
+            k = self.k_norm(k)
+
+            # q, k = self.rotary_emb(positions, q, k)
+            cos, sin = position_embeddings
+            q, k = apply_rotary_pos_emb(q, k, cos, sin)
+
+            # [b, h, s, head_dim] ->  [b, s, h, head_dim]
+            q = q.permute(0, 2, 1, 3)
+            k = k.permute(0, 2, 1, 3)
 
         attn_output = self.attn(q, k, v, forward_batch=forward_batch)
 
